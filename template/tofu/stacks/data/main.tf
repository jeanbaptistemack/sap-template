# =============================================================================
# Data — Ressources data (Storage, SQL, Fabric)
# Usage: just tofu-plan STACK=data ENV=dev
# =============================================================================

# TODO: Define your data resources here
# Example:
# resource "azurerm_resource_group" "data" {
#   name     = "rg-${var.project}-data-${var.environment}"
#   location = var.location
# }
